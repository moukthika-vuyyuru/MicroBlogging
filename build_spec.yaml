version: 1.0
build-stages:
  - build:
      stage:
        name: Build Stage
        id: build
      steps:
        - name: Maven Build
          type: plugin
          plugin: oci/pipeline-plugin-devops-commons:2.0.3
          stage_output: build
          script:
            - mvn clean install
